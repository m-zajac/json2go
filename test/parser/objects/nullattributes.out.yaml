- options:
    skipEmptyKeys: false
  out: |
    type Document []struct {
      A int `json:"a"`
      B interface{} `json:"b"`
    }

- options:
    skipEmptyKeys: true
  out: |
    type Document []struct {
      A int `json:"a"`
    }