- options:
    makeMaps: true
    makeMapsWhenMinAttributes: 5
    extractCommonTypes: false
  out: |
    type Document struct {
    	K1 map[string]Nested  `json:"k1"`
    	K2 map[string]Nested2 `json:"k2"`
    }
    
    type Nested struct {
    	Ok bool `json:"ok"`
    }
    
    type Nested2 struct {
    	Ok bool `json:"ok"`
    }
- options:
    makeMaps: true
    makeMapsWhenMinAttributes: 5
    extractCommonTypes: true
  out: |
    type Document struct {
    	K1 map[string]Ok `json:"k1"`
    	K2 map[string]Ok `json:"k2"`
    }
    
    type Ok struct {
    	Ok bool `json:"ok"`
    }
