- options:
    makeMaps: true
    makeMapsWhenMinAttributes: 5
    extractCommonTypes: false
  out: |
    type Document struct {
      K1 map[string]struct {
        Ok bool `json:"ok"`
      } `json:"k1"`
      K2 map[string]struct {
        Ok bool `json:"ok"`
      } `json:"k2"`
    }

- options:
    makeMaps: true
    makeMapsWhenMinAttributes: 5
    extractCommonTypes: true
  out: |
    type Document struct {
      K1 K `json:"k1"`
      K2 K `json:"k2"`
    }

    type K map[string]struct {
      Ok bool `json:"ok"`
    }