- options:
    extractCommonTypes: true
  out: |
    type Document struct {
    	BillingAddress  Addres `json:"billing_address"`
    	ShippingAddress Addres `json:"shipping_address"`
    }
    
    type Addres struct {
    	City   string `json:"city"`
    	Street string `json:"street"`
    }
